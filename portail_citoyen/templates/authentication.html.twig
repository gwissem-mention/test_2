{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ 'to.log.in'|trans }}</h1>
    <div class="fr-connect-group">
        <button class="fr-connect" id="france_connect_auth_button">
            <span class="fr-connect__login">{{ 'identify.with'|trans }}</span>
            <span class="fr-connect__brand">{{ 'france.connect'|trans }}</span>
        </button>
        <p>
            <a href="https://franceconnect.gouv.fr/" target="_blank"  rel="noopener" title="{{ 'what.is.france.connect'|trans }} - {{ 'new.window'|trans }}">{{ 'what.is.france.connect'|trans }}</a>
        </p>
    </div>
    <a href="{{ path('complaint_identity') }}">{{ 'continue.pel.without.log.in'|trans }}</a>
    <p>{{ 'continue.pel.without.log.in.explanation'|trans }}</p>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        let path = "{{ path('complaint_identity') }}";
        document.getElementById('france_connect_auth_button').addEventListener('click', function () {
            window.location.href = path;
        })
    </script>
{% endblock %}
