---
- hosts: all

  become: true

  environment:
    APP_ENV: prod
    REDIS_HOST: 127.0.0.1
    FILE_CITIES: insee_commune.csv

  vars_files:
    - ../vars/vars.yaml
    - ../vault/vault.yaml

  roles:
    - ansistrano.deploy

