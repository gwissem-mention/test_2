framework:
    http_client:
        scoped_clients:
            oodrive.client:
                scope: 'https://sharing\.oodrive\.com'
                base_uri: '%env(OODRIVE_BASE_URI)%'
                headers:
                    Accept: 'application.json'
                    Content-Type: 'application.json'
