<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}" title="{{ 'pel.home'|trans }}">
            <img id="image_logo" src="{{ asset('build/images/logo.png') }}" alt="{{ 'pel.online.complaint'|trans }}"/>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
            <div class="d-flex flex-column navbar-nav me-auto mb-2 mb-lg-0">
                <h1>{{ 'pel.agent.complaint.online'|trans }}</h1>
                <p>{{ 'pel.header.baseline'|trans }}</p>
            </div>
            <form class="d-flex me-3 mb-2 mb-lg-0" role="search">
                <input class="form-control me-2" type="search" placeholder="{{ 'pel.search'|trans }}" aria-label="{{ 'pel.search'|trans }}">
                <button class="btn btn-outline-success" type="submit">{{ 'pel.search'|trans }}</button>
            </form>
            {% if app.user %}
                <div class="d-flex align-items-center text-center text-md-end">
                    <div class="dropdown">
                        <a class="d-block text-secondary text-decoration-none m-3" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-bell-fill fs-2"></i>
                            <span class="badge bg-danger rounded-pill--up">3</span>
                        </a>
                        <ul class="dropdown-menu">
                            <li>
                                <div class="dropdown-header">{{ 'pel.notifications'|trans }}</div>
                            </li>
                            <li>
                                {# TO DO : remove this text and replace it with real notifications #}
                                <a class="dropdown-item" href="#">Une première notification !</a>
                            </li>
                            <li>
                                {# TO DO : remove this text and replace it with real notifications #}
                                <a class="dropdown-item" href="#">Une deuxième notification !</a>
                            </li>
                            <li>
                                {# TO DO : remove this text and replace it with real notifications #}
                                <a class="dropdown-item" href="#">Une troisième notification !</a>
                            </li>
                        </ul>
                    </div>
                    <div class="avatar bg-secondary rounded-circle text-light m-2">
                        {{ app.user.initials }}
                    </div>
                    {% if not sso_is_enabled %}
                        <a href="{{ path('app_sso_less_close_session') }}" title="{{ 'pel.logout'|trans }}" class="text-secondary m-3">
                            <i class="bi bi-box-arrow-right"></i>
                        </a>
                    {% endif %}
                </div>
            {% endif %}
        </div>
    </div>
</nav>
