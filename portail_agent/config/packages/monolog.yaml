monolog:
    channels:
        - deprecation # Deprecations are logged in the dedicated "deprecation" channel when it exists
        - oodrive # Logs dedicated to communication with the Oodrive API
        - application_traces_gn # Logs dedicated to the application traces for the GN institution
        - application_traces_pn # Logs dedicated to the application traces for the GN institution
        - api # Logs dedicated to our API endpoints
        - salesforce # Logs dedicated to communication with the Salesforce API

when@dev:
    monolog:
        handlers:
            main:
                type: rotating_file
                path: "%kernel.logs_dir%/%kernel.environment%.log"
                level: debug
                channels: [ "!event", "!oodrive", "!application_traces_gn", "!application_traces_pn" ]
                max_files: 20
            # uncomment to get logging in your browser
            # you may have to allow bigger header sizes in your Web server configuration
            #firephp:
            #    type: firephp
            #    level: info
            #chromephp:
            #    type: chromephp
            #    level: info
            console:
                type: console
                process_psr_3_messages: false
                channels: [ "!event", "!doctrine", "!console", "!oodrive", "!application_traces_gn", "!application_traces_pn" ]
            oodrive:
                type: rotating_file
                path: "%kernel.logs_dir%/oodrive_%kernel.environment%.log"
                level: debug
                channels: [ "oodrive" ]
                max_files: 20
            application_traces_gn:
                type: rotating_file
                path: "%kernel.logs_dir%/application_traces_gn_%kernel.environment%.log"
                level: debug
                channels: [ "application_traces_gn" ]
                formatter: 'app.logger.formatter.json_traces'
                max_files: 20
            application_traces_pn:
                type: rotating_file
                path: "%kernel.logs_dir%/application_traces_pn_%kernel.environment%.log"
                level: debug
                channels: [ "application_traces_pn" ]
                formatter: 'app.logger.formatter.json_traces'
                max_files: 20
            api:
                type: rotating_file
                path: "%kernel.logs_dir%/api_%kernel.environment%.log"
                level: debug
                channels: [ "api" ]
                max_files: 20    
            salesforce:
                type: rotating_file
                path: "%kernel.logs_dir%/salesforce_%kernel.environment%.log"
                level: debug
                channels: [ "salesforce" ]
                max_files: 20    


when@test:
    monolog:
        handlers:
            main:
                type: fingers_crossed
                action_level: error
                handler: nested
                excluded_http_codes: [404, 405]
                channels: ["!event"]
            nested:
                type: stream
                path: "%kernel.logs_dir%/%kernel.environment%.log"
                level: debug

when@prod: &prod
    monolog:
        handlers:
            main:
                type: fingers_crossed
                action_level: error
                handler: nested
                excluded_http_codes: [404, 405]
                buffer_size: 50 # How many messages should be saved? Prevent memory leaks
            nested:
                type: stream
                path: php://stderr
                level: debug
                formatter: monolog.formatter.json
            file_log:
                type: rotating_file
                path: "%kernel.logs_dir%/%kernel.environment%.log"
                level: error
                channels: [ "!oodrive" ]
                max_files: 20
            oodrive_log:
                type: rotating_file
                path: "%kernel.logs_dir%/oodrive_%kernel.environment%.log"
                level: error
                channels: [ "oodrive" ]
                max_files: 20
            application_traces_gn_log:
                type: rotating_file
                path: "%kernel.logs_dir%/application_traces_gn_%kernel.environment%.log"
                level: info
                channels: [ "application_traces_gn" ]
                formatter: 'app.logger.formatter.json_traces'
                max_files: 20
            application_traces_pn_log:
                type: rotating_file
                path: "%kernel.logs_dir%/application_traces_pn_%kernel.environment%.log"
                level: info
                channels: [ "application_traces_pn" ]
                formatter: 'app.logger.formatter.json_traces'
                max_files: 20
            api_log:
                type: rotating_file
                path: "%kernel.logs_dir%/api_%kernel.environment%.log"
                level: info
                channels: [ "api" ]
                max_files: 20
            console:
                type: console
                process_psr_3_messages: false
                channels: ["!event", "!doctrine"]
            deprecation:
                type: stream
                channels: [deprecation]
                path: php://stderr
            salesforce_log:
                type: rotating_file
                path: "%kernel.logs_dir%/salesforce_%kernel.environment%.log"
                level: error
                channels: [ "salesforce" ]
                max_files: 20    

when@e2e: *prod
