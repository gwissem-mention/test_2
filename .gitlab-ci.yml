variables:
    HARBOR_PROJECT: "ministere-de-linterieur-plaintes-en-ligne-pel-2"
    PHP_AGENT_ARTIFACT_NAME: "php-agent:$CI_COMMIT_REF_SLUG"
    PHP_CITOYEN_ARTIFACT_NAME: "php-citoyen:$CI_COMMIT_REF_SLUG"
    PHP_AGENT_CHANGE: 0
    PHP_CITOYEN_CHANGE: 0
    KUBERNETES_MEMORY_REQUEST: 1Gi
    KUBERNETES_MEMORY_LIMIT: 1Gi

workflow:
    rules:
        - if: $CI_PIPELINE_SOURCE == "merge_request_event"
        - if: $CI_COMMIT_TAG
        - if: $CI_COMMIT_BRANCH == "develop"
        - if: $CI_COMMIT_BRANCH == "main"

stages:
    - check
    - build
    - security
    - quality
    - test

#######################################
##         Basic Inheritance         ##
#######################################

.basic-override: &basic-override
    interruptible: true
    tags:
        - docker
        - rancher

.agent-step: &agent-step
    image: "registry.smile.fr/${HARBOR_PROJECT}/${PHP_AGENT_ARTIFACT_NAME}"
    interruptible: true
    before_script:
        - cd ./portail_agent/
    <<: *basic-override

.frontend-step: &frontend-step
    image: node:16
    <<: *basic-override

.citoyen-frontend-step: &citoyen-frontend-step
    <<: *frontend-step
    interruptible: true
    before_script:
        - cd ./portail_citoyen/

.agent-frontend-step: &agent-frontend-step
    <<: *frontend-step
    interruptible: true
    before_script:
        - cd ./portail_agent/

.citoyen-step: &citoyen-step
    image: "registry.smile.fr/${HARBOR_PROJECT}/${PHP_CITOYEN_ARTIFACT_NAME}"
    interruptible: true
    before_script:
        - cd ./portail_citoyen/
    <<: *basic-override

#######################################
##               Check               ##
#######################################

check-php-agent-change:
    stage: check
    image: docker
    script:
        - echo 'PHP_AGENT_CHANGE=1' >> build.env
    <<: *basic-override
    artifacts:
        reports:
            dotenv: build.env
    only:
        changes:
            - docker-compose.yml
            - ./portail_agent/Dockerfile
            - ./portail_agent/docker/**/*

check-php-citoyen-change:
    stage: check
    image: docker
    script:
        - echo 'PHP_CITOYEN_CHANGE=1' >> build.env
    only:
        changes:
            - docker-compose.yml
            - ./portail_citoyen/Dockerfile
            - ./portail_citoyen/docker/**/*
    <<: *basic-override
    artifacts:
        reports:
            dotenv: build.env

check-php-images:
    stage: check
    image: docker
    variables:
        PHP_AGENT_CHANGE: 0
        PHP_CITOYEN_CHANGE: 0
    script:
        - echo "${HARBOR_PASSWORD}" | docker login registry.smile.fr -u ${HARBOR_USER} --password-stdin
        - docker manifest inspect registry.smile.fr/${HARBOR_PROJECT}/${PHP_AGENT_ARTIFACT_NAME} && echo 'PHP_AGENT_EXIST=1' >> build.env || echo 'PHP_AGENT_EXIST=0' >> build.env && true
        - docker manifest inspect registry.smile.fr/${HARBOR_PROJECT}/${PHP_CITOYEN_ARTIFACT_NAME} && echo 'PHP_CITOYEN_EXIST=1' >> build.env || echo 'PHP_CITOYEN_EXIST=0' >> build.env && true
    <<: *basic-override
    artifacts:
        reports:
            dotenv: build.env
    dependencies:
        - check-php-agent-change
        - check-php-citoyen-change

#######################################
##               Build               ##
#######################################

agent-build-php:
    image:
        name: gcr.io/kaniko-project/executor:debug
        entrypoint: [ "" ]
    stage: build
    variables:
        PHP_AGENT_CHANGE: 0
        PHP_AGENT_EXIST: 0
    script:
        - echo "PHP_AGENT_CHANGE=${PHP_AGENT_CHANGE}"
        - echo "PHP_AGENT_EXIST=${PHP_AGENT_EXIST}"
        - mkdir -p /kaniko/.docker
        - |
            cat > /kaniko/.docker/config.json <<EOM
            {
                "auths": {
                    "registry.smile.fr": {
                        "auth": "$(printf "%s:%s" "${HARBOR_USER}" "${HARBOR_PASSWORD}" | base64 | tr -d '\n')"
                    }
                }
            }
            EOM
        - |
            if [ ${PHP_AGENT_CHANGE} -eq 1 ] || [ ${PHP_AGENT_EXIST} -eq 0 ];
            then
                echo "Build of PHP Agent needed"

                /kaniko/executor --context "${CI_PROJECT_DIR}/portail_agent/" --build-arg "BUILD_PATH='./portail_agent/'" --target "symfony_php" --destination "registry.smile.fr/${HARBOR_PROJECT}/${PHP_AGENT_ARTIFACT_NAME}"
            else
                echo "Build of PHP Agent not needed"
            fi
    <<: *basic-override
    dependencies:
        - check-php-images

citoyen-build-php:
    image:
        name: gcr.io/kaniko-project/executor:debug
        entrypoint: [ "" ]
    stage: build
    variables:
        PHP_CITOYEN_CHANGE: 0
        PHP_CITOYEN_EXIST: 0
    script:
        - echo "PHP_CITOYEN_CHANGE=${PHP_CITOYEN_CHANGE}"
        - echo "PHP_CITOYEN_EXIST=${PHP_CITOYEN_EXIST}"
        - mkdir -p /kaniko/.docker
        - |
            cat > /kaniko/.docker/config.json <<EOM
            {
                "auths": {
                    "registry.smile.fr": {
                        "auth": "$(printf "%s:%s" "${HARBOR_USER}" "${HARBOR_PASSWORD}" | base64 | tr -d '\n')"
                    }
                }
            }
            EOM
        - >-
            if [ ${PHP_CITOYEN_CHANGE} -eq 1 ] || [ ${PHP_CITOYEN_EXIST} -eq 0 ];
            then
                echo "Build of PHP Citoyen needed"
                /kaniko/executor --context "${CI_PROJECT_DIR}/portail_citoyen/" --build-arg "BUILD_PATH='./portail_citoyen/'" --target "symfony_php" --destination "registry.smile.fr/${HARBOR_PROJECT}/${PHP_CITOYEN_ARTIFACT_NAME}"
            else
                echo "Build of PHP Citoyen not needed"
            fi
    <<: *basic-override
    dependencies:
        - check-php-images

.build-tools: &build-tools
    stage: build
    script:
        - make tools-install

agent-build-tools:
    <<: *agent-step
    <<: *build-tools
    needs:
        - agent-build-php
    artifacts:
        paths:
            - ./portail_agent/tools

citoyen-build-tools:
    <<: *citoyen-step
    <<: *build-tools
    needs:
        - citoyen-build-php
    artifacts:
        paths:
            - ./portail_citoyen/tools

.build-vendor: &build-vendor
    stage: build
    script:
        - make vendor

agent-build-vendor:
    <<: *agent-step
    <<: *build-vendor
    needs:
        - agent-build-php
    artifacts:
        paths:
            - ./portail_agent/vendor

citoyen-build-vendor:
    <<: *citoyen-step
    <<: *build-vendor
    needs:
        - citoyen-build-php
    artifacts:
        paths:
            - ./portail_citoyen/vendor

.build-front: &build-front
    stage: build
    script:
        - make install
        - make build-ci

agent-build-front:
    <<: *build-front
    <<: *agent-frontend-step
    <<: *basic-override
    needs:
        - agent-build-vendor
    artifacts:
        paths:
            - ./portail_agent/public/build
            - ./portail_agent/node_modules

citoyen-build-front:
    <<: *build-front
    <<: *basic-override
    <<: *citoyen-frontend-step
    variables:
        KUBERNETES_MEMORY_REQUEST: 1Gi
        KUBERNETES_MEMORY_LIMIT: 1Gi
    needs:
        - citoyen-build-vendor
    artifacts:
        paths:
            - ./portail_citoyen/public/build
            - ./portail_citoyen/node_modules

#######################################
##            Quality Tools           #
#######################################

## Security ##
.symfony-security: &symfony-security
    stage: security
    script:
        - make security

agent-symfony-security:
    <<: *agent-step
    <<: *symfony-security

citoyen-symfony-security:
    <<: *citoyen-step
    <<: *symfony-security
    
.tools-security: &tools-security
    stage: security
    script:
        - make tools-security

agent-tools-security:
    <<: *agent-step
    <<: *tools-security

citoyen-tools-security:
    <<: *citoyen-step
    <<: *tools-security

.tools-outdated: &tools-outdated
    stage: security
    script:
        - make tools-outdated

agent-tools-outdated:
    <<: *agent-step
    <<: *tools-security

citoyen-tools-outdated:
    <<: *citoyen-step
    <<: *tools-security

.frontend-audit: &frontend-audit
    stage: security
    script:
        - make frontend-audit

#citoyen-frontend-security:
#    <<: *frontend-audit
#    <<: *citoyen-frontend-step
#
#agent-frontend-security:
#    <<: *frontend-audit
#    <<: *agent-frontend-step

## Php Cs Fixer ##
.cs: &cs
    stage: quality
    script:
        - make cs

agent-php-cs:
    <<: *agent-step
    <<: *cs

citoyen-php-cs:
    <<: *citoyen-step
    <<: *cs

.frontend-twig-cs: &frontend-twig-cs
    stage: quality
    script:
        - make APP_ENV=test frontend-twig-cs-test

citoyen-frontend-twig-cs:
    <<: *citoyen-step
    <<: *frontend-twig-cs

agent-frontend-twig-cs:
    <<: *agent-step
    <<: *frontend-twig-cs

.frontend-cs: &frontend-cs
    stage: quality
    script:
        - make frontend-cs-test

citoyen-frontend-cs:
    <<: *citoyen-frontend-step
    <<: *frontend-cs

agent-frontend-cs:
    <<: *agent-frontend-step
    <<: *frontend-cs

.frontend-cpd: &frontend-cpd
    stage: quality
    script:
        - make frontend-cpd-test

#citoyen-frontend-cpd:
#    <<: *citoyen-frontend-step
#    <<: *frontend-cpd
#
#agent-frontend-cpd:
#    <<: *agent-frontend-step
#    <<: *frontend-cpd

## Php Magic Number Detector ##
.mnd: &mnd
    stage: quality
    script:
        - make mnd

agent-php-mnd:
    <<: *agent-step
    <<: *mnd

citoyen-php-mnd:
    <<: *citoyen-step
    <<: *mnd

## Phpstan ##
.phpstan: &phpstan
    stage: quality
    script:
        - make phpstan

agent-php-phpstan:
    <<: *agent-step
    <<: *phpstan

citoyen-php-phpstan:
    <<: *citoyen-step
    <<: *phpstan

## Php Copy/Past Detector ##
.cpd: &cpd
    stage: quality
    script:
        - make cpd

agent-php-cpd:
    <<: *agent-step
    <<: *cpd

citoyen-php-cpd:
    <<: *citoyen-step
    <<: *cpd

#######################################
##          Tests Frameworks          #
#######################################

## PhpUnit ##
.unit: &unit
    stage: test
    script:
        - make unit

agent-unit:
    <<: *agent-step
    <<: *unit
    variables:
        APP_ENV: "test"
        POSTGRES_DB: "app_test"
        POSTGRES_PASSWORD: "ChangeMe"
        POSTGRES_USER: "symfony"
        POSTGRES_HOST: "127.0.0.1"
    script:
        - make APP_ENV=${APP_ENV} db-ci-setup
        - make APP_ENV=${APP_ENV} db-ci-load
        - make APP_ENV=${APP_ENV} referential-create
        - make APP_ENV=${APP_ENV} referential-ci-load
        - make unit
    services:
        -   name: postgres:13-alpine
            alias: database-unit-agent

citoyen-unit:
    <<: *citoyen-step
    <<: *unit
    variables:
        APP_ENV: "test"
        POSTGRES_DB: "app_test"
        POSTGRES_PASSWORD: "ChangeMe"
        POSTGRES_USER: "symfony"
        POSTGRES_HOST: "127.0.0.1"
    script:
        - make APP_ENV=${APP_ENV} db-ci-setup
        - make APP_ENV=${APP_ENV} referential-create
        - make APP_ENV=${APP_ENV} referential-ci-load
        - make unit
    services:
        -   name: postgres:13-alpine
            alias: database-unit-citoyen

## Behat ##
.func: &func
    stage: test
    script:
        - make behat

agent-func:
    <<: *agent-step
    <<: *func
    variables:
        APP_ENV: "test"
        POSTGRES_DB: "app_test"
        POSTGRES_PASSWORD: "ChangeMe"
        POSTGRES_USER: "symfony"
        POSTGRES_HOST: "127.0.0.1"
    script:
        - make APP_ENV=${APP_ENV} db-ci-setup
        - make APP_ENV=${APP_ENV} db-ci-load
        - make APP_ENV=${APP_ENV} referential-create
        - make APP_ENV=${APP_ENV} referential-ci-load
        - make behat
    services:
        - name: postgres:13-alpine
          alias: database-func-agent
    artifacts:
        name: "agent-func-$CI_COMMIT_REF_NAME"
        paths:
            - /builds/ministere-de-l-interieur/plaintes-en-ligne-pel/portail_agent/var/log
            - /builds/ministere-de-l-interieur/plaintes-en-ligne-pel/portail_agent/build/screenshots
        expire_in: 1 week
        when: on_failure

citoyen-func:
    <<: *citoyen-step
    <<: *func
    variables:
        APP_ENV: "test"
        POSTGRES_DB: "app_test"
        POSTGRES_PASSWORD: "ChangeMe"
        POSTGRES_USER: "symfony"
        POSTGRES_REFERENTIAL_HOST: "127.0.0.1"
        POSTGRES_REFERENTIAL_DB: "reference"
        POSTGRES_REFERENTIAL_PASSWORD: "ChangeMe"
        POSTGRES_REFERENTIAL_USER: "symfony"
    script:
        - make APP_ENV=${APP_ENV} db-ci-setup
        - make APP_ENV=${APP_ENV} referential-ci-create
        - make APP_ENV=${APP_ENV} referential-ci-load
        - make behat
    services:
        - name: postgres:13-alpine
          alias: database-func-citoyen
    artifacts:
        name: "citoyen-func-$CI_COMMIT_REF_NAME"
        paths:
            - /builds/ministere-de-l-interieur/plaintes-en-ligne-pel/portail_citoyen/var/log
            - /builds/ministere-de-l-interieur/plaintes-en-ligne-pel/portail_citoyen/build/screenshots
        expire_in: 1 week
        when: on_failure

citoyen-e2e:
    <<: *citoyen-step
    variables:
        APP_ENV: "e2e"
        POSTGRES_DB: "app"
        POSTGRES_PASSWORD: "ChangeMe"
        POSTGRES_USER: "symfony"
        POSTGRES_REFERENTIAL_HOST: "127.0.0.1"
        POSTGRES_REFERENTIAL_DB: "reference"
        POSTGRES_REFERENTIAL_PASSWORD: "ChangeMe"
        POSTGRES_REFERENTIAL_USER: "symfony"
    script:
        - php bin/console cache:clear --env=${APP_ENV}
        - echo "APP_ENV=${APP_ENV}" >> .env.local
        - php -S 127.0.0.1:8080 -t public/ > /dev/null 2>&1 &
        - sed -i "s/nginx-citoyen/127.0.0.1:8080/g" behat.yml.dist
        - sed -i "s/chrome:4444/chrome-citoyen:4444/g" behat.yml.dist
        - make APP_ENV=${APP_ENV} db-ci-setup
        - make APP_ENV=${APP_ENV} referential-ci-create
        - make APP_ENV=${APP_ENV} referential-ci-load
        - make leftover-e2e
    artifacts:
        name: "citoyen-e2e-$CI_COMMIT_REF_NAME"
        paths:
            - /builds/ministere-de-l-interieur/plaintes-en-ligne-pel/portail_citoyen/var/log
            - /builds/ministere-de-l-interieur/plaintes-en-ligne-pel/portail_citoyen/build/screenshots
        expire_in: 1 week
        when: on_failure
    services:
        -   name: postgres:13-alpine
            alias: database-e2e-citoyen
        -   name: selenium/standalone-chrome:4.5.3
            alias: chrome-citoyen
        -   name: redislabs/rejson:latest
            alias: redis-citoyen

citoyen-complaint-e2e:
    <<: *citoyen-step
    variables:
        APP_ENV: "e2e"
        POSTGRES_DB: "app"
        POSTGRES_PASSWORD: "ChangeMe"
        POSTGRES_USER: "symfony"
        POSTGRES_REFERENTIAL_HOST: "127.0.0.1"
        POSTGRES_REFERENTIAL_DB: "reference"
        POSTGRES_REFERENTIAL_PASSWORD: "ChangeMe"
        POSTGRES_REFERENTIAL_USER: "symfony"
    script:
        - php bin/console cache:clear --env=${APP_ENV}
        - echo "APP_ENV=${APP_ENV}" >> .env.local
        - php -S 127.0.0.1:8080 -t public/ > /dev/null 2>&1 &
        - sed -i "s/nginx-citoyen/127.0.0.1:8080/g" behat.yml.dist
        - make APP_ENV=${APP_ENV} db-ci-setup
        - make APP_ENV=${APP_ENV} referential-ci-create
        - make APP_ENV=${APP_ENV} referential-ci-load
        - make complaint-e2e
    artifacts:
        name: "citoyen-flaky-e2e-$CI_COMMIT_REF_NAME"
        paths:
            - /builds/ministere-de-l-interieur/plaintes-en-ligne-pel/portail_citoyen/var/log
            - /builds/ministere-de-l-interieur/plaintes-en-ligne-pel/portail_citoyen/build/screenshots
        expire_in: 1 week
        when: on_failure
    services:
        -   name: postgres:13-alpine
            alias: database-e2e-citoyen
        -   name: selenium/standalone-chrome:4.5.3
            alias: chrome
        -   name: redislabs/rejson:latest
            alias: redis-citoyen

citoyen-complete-workflow-e2e:
    <<: *citoyen-step
    variables:
        APP_ENV: "e2e"
        POSTGRES_DB: "app"
        POSTGRES_PASSWORD: "ChangeMe"
        POSTGRES_USER: "symfony"
        POSTGRES_REFERENTIAL_HOST: "127.0.0.1"
        POSTGRES_REFERENTIAL_DB: "reference"
        POSTGRES_REFERENTIAL_PASSWORD: "ChangeMe"
        POSTGRES_REFERENTIAL_USER: "symfony"
    script:
        - php bin/console cache:clear --env=${APP_ENV}
        - echo "APP_ENV=${APP_ENV}" >> .env.local
        - php -S 127.0.0.1:8080 -t public/ > /dev/null 2>&1 &
        - sed -i "s/nginx-citoyen/127.0.0.1:8080/g" behat.yml.dist
        - make APP_ENV=${APP_ENV} db-ci-setup
        - make APP_ENV=${APP_ENV} referential-ci-create
        - make APP_ENV=${APP_ENV} referential-ci-load
        - make complete-workflow-e2e
    artifacts:
        name: "citoyen-flaky-e2e-$CI_COMMIT_REF_NAME"
        paths:
            - /builds/ministere-de-l-interieur/plaintes-en-ligne-pel/portail_citoyen/var/log
            - /builds/ministere-de-l-interieur/plaintes-en-ligne-pel/portail_citoyen/build/screenshots
        expire_in: 1 week
        when: on_failure
    services:
        -   name: postgres:13-alpine
            alias: database-e2e-citoyen
        -   name: selenium/standalone-chrome:4.5.3
            alias: chrome
        -   name: redislabs/rejson:latest
            alias: redis-citoyen

citoyen-flaky-e2e:
    <<: *citoyen-step
    variables:
        APP_ENV: "e2e"
        POSTGRES_DB: "app"
        POSTGRES_PASSWORD: "ChangeMe"
        POSTGRES_USER: "symfony"
        POSTGRES_REFERENTIAL_HOST: "127.0.0.1"
        POSTGRES_REFERENTIAL_DB: "reference"
        POSTGRES_REFERENTIAL_PASSWORD: "ChangeMe"
        POSTGRES_REFERENTIAL_USER: "symfony"
    script:
        - php bin/console cache:clear --env=${APP_ENV}
        - echo "APP_ENV=${APP_ENV}" >> .env.local
        - php -S 127.0.0.1:8080 -t public/ > /dev/null 2>&1 &
        - sed -i "s/nginx-citoyen/127.0.0.1:8080/g" behat.yml.dist
        - make APP_ENV=${APP_ENV} db-ci-setup
        - make APP_ENV=${APP_ENV} referential-ci-create
        - make APP_ENV=${APP_ENV} referential-ci-load
        - make flaky-e2e
    artifacts:
        name: "citoyen-flaky-e2e-$CI_COMMIT_REF_NAME"
        paths:
            - /builds/ministere-de-l-interieur/plaintes-en-ligne-pel/portail_citoyen/var/log
            - /builds/ministere-de-l-interieur/plaintes-en-ligne-pel/portail_citoyen/build/screenshots
        expire_in: 1 week
        when: on_failure
    services:
        -   name: postgres:13-alpine
            alias: database-e2e-citoyen
        -   name: selenium/standalone-chrome:4.5.3
            alias: chrome
        -   name: redislabs/rejson:latest
            alias: redis-citoyen

agent-e2e:
    <<: *agent-step
    variables:
        APP_ENV: "e2e"
        POSTGRES_DB: "app"
        POSTGRES_PASSWORD: "ChangeMe"
        POSTGRES_USER: "symfony"
        POSTGRES_HOST: "127.0.0.1"
    script:
        - php bin/console cache:clear --env=${APP_ENV}
        - echo "APP_ENV=${APP_ENV}" >> .env.local
        - php -S 127.0.0.1:8080 -t  public/ > /dev/null 2>&1 &
        - sed -i "s/nginx-agent/127.0.0.1:8080/g" behat.yml.dist
        - make APP_ENV=${APP_ENV} db-ci-setup
        - make APP_ENV=${APP_ENV} db-ci-load
        - make APP_ENV=${APP_ENV} referential-create
        - make APP_ENV=${APP_ENV} referential-ci-load
        - make e2e
    services:
        -   name: postgres:13-alpine
            alias: database-e2e-agent
        -   name: selenium/standalone-chrome:4.5.3
            alias: chrome
    artifacts:
        name: "agent-e2e-$CI_COMMIT_REF_NAME"
        paths:
            - /builds/ministere-de-l-interieur/plaintes-en-ligne-pel/portail_agent/var/log
            - /builds/ministere-de-l-interieur/plaintes-en-ligne-pel/portail_agent/build/screenshots
        expire_in: 1 week
        when: on_failure

agent-flaky-e2e:
    <<: *agent-step
    variables:
        APP_ENV: "e2e"
        POSTGRES_DB: "app"
        POSTGRES_PASSWORD: "ChangeMe"
        POSTGRES_USER: "symfony"
        POSTGRES_HOST: "127.0.0.1"
    script:
        - php bin/console cache:clear --env=${APP_ENV}
        - echo "APP_ENV=${APP_ENV}" >> .env.local
        - php -S 127.0.0.1:8080 -t  public/ > /dev/null 2>&1 &
        - sed -i "s/nginx-agent/127.0.0.1:8080/g" behat.yml.dist
        - make APP_ENV=${APP_ENV} db-ci-setup
        - make APP_ENV=${APP_ENV} db-ci-load
        - make APP_ENV=${APP_ENV} referential-create
        - make APP_ENV=${APP_ENV} referential-load
        - make flaky-e2e
    services:
        -   name: postgres:13-alpine
            alias: database-e2e-agent
        -   name: selenium/standalone-chrome:4.5.3
            alias: chrome
    artifacts:
        name: "agent-flaky-e2e-$CI_COMMIT_REF_NAME"
        paths:
            - /builds/ministere-de-l-interieur/plaintes-en-ligne-pel/portail_agent/var/log
            - /builds/ministere-de-l-interieur/plaintes-en-ligne-pel/portail_agent/build/screenshots
        expire_in: 1 week
        when: on_failure
